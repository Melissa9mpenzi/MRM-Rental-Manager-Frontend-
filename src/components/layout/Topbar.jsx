export function Topbar({ title }) {
  return (
    <div className="mb-4 flex items-center justify-between">
      <div className="text-xl font-semibold text-brand-dark">{title}</div>
    </div>
  )
}

